# 📄 Техническое Задание (ТЗ): Интерактивный 2.5D Манекен (Гибридный Пайплайн)

## I. Общее Описание Проекта

### 1.1. Цель

Создание высокопроизводительного, фотореалистичного веб-модуля для просмотра костюмов в формате 360° с интерактивным управлением мышью. Система должна быть максимально надежной, работать с **заранее сгенерированными статическими ассетами** и поддерживать динамическую смену цвета/размера.

### 1.2. Стек Технологий

| Слой                 | Технология                                   | Назначение                                                                   |
| :------------------- | :------------------------------------------- | :--------------------------------------------------------------------------- |
| **Backend / API**    | **NestJS** (TypeScript, Node.js-Bun)         | Управление данными (модели, SKU), API для обслуживания конфигурации ассетов. |
| **Frontend / UI**    | **Next.js** (React, TypeScript)              | Пользовательский интерфейс, логика загрузки данных, рендеринг 2.5D-вьюера.   |
| **Рендеринг**        | **PixiJS** / HTML5 Canvas                    | Аппаратно-ускоренный композитинг слоев и анимация.                           |
| **Ассеты (Offline)** | **Generative AI** (Krea/Nano Banano), Python | Инструменты для генерации недостающих кадров и сборки спрайт-листов.         |

---

## II. Архитектура Стека и Разделение Ответственности

### 2.1. Backend: NestJS (Data & Static Asset Manager) ⚙️

NestJS выполняет роль быстрого API для обслуживания конфигурации. **NestJS не имеет зависимостей от AI в рантайме.**

- **API-Шлюз:** Предоставление защищенного доступа к конфигурационным данным (`JSON-манифест`).
- **Управление данными:** Хранение метаданных (цвета, размеры, фасоны) и **прямых URL-ссылок** на готовые ассеты, расположенные на CDN/S3.
- **Надежность:** Сервис является максимально стабильным, поскольку зависит только от БД (PostgreSQL/MongoDB) и внешнего хранилища (S3).

### 2.2. Frontend: Next.js (Rendering Layer) ⚛️

Next.js обеспечивает производительность загрузки и обрабатывает всю интерактивность на стороне клиента.

- **Страницы Продукта (SSR/SSG/ISR):** Основная страница продукта и UI (селекторы цвета/размера) должны быть сгенерированы на сервере (для SEO и скорости загрузки).
- **Интерактивный Вьюер (Client Component):** Компонент `MannequinViewer` с PixiJS **должен быть** клиентским компонентом (из-за необходимости доступа к Canvas, GPU и обработке мыши).
- **Загрузка:** Загрузка Sprite Sheets происходит по прямой ссылке с CDN.

---

## III. Пайплайн Создания Ассетов (Offline Batch Processing)

Этап выполняется **до** развертывания кода в продакшене.

1.  **Съемка (Base Assets):**
    - Для каждого SKU (`Модель` + `Цвет`) делаются только **два высококачественных фото**: вид строго **спереди (0°)** и вид строго **сзади (180°)**.
2.  **Генеративная Интерполяция:**
    - Используя выбранный AI-инструмент (Krea, ControlNet и т.д.), генерируется **последовательность из ~34-70 кадров**, заполняющих вращение между 0° и 180° (и 180° и 360°).
    - **Цель:** Сохранение текстуры, света и геометрии, заданных двумя реальными кадрами.
3.  **Сборка Sprite Sheet:**
    - Скрипт (Node.js/Python) **склеивает все сгенерированные и реальные кадры** (36/72 кадров) в один оптимизированный файл **Sprite Sheet** (WebP/JPEG).
    - **Финальный Шаг:** Sprite Sheet загружается в S3/CDN.

---

## IV. Модули и Функционал

### 4.1. Backend (NestJS Modules)

| Модуль                     | Описание                                                                                                                       |
| :------------------------- | :----------------------------------------------------------------------------------------------------------------------------- |
| **`ProductCatalogModule`** | CRUD-операции для моделей, цветов, размеров и SKU.                                                                             |
| **`ManifestModule`**       | **Ключевой API:** `GET /api/v1/models/:id/manifest`. Выдача JSON-конфигурации, содержащей все SKU и URL готовых спрайт-листов. |
| **`StorageModule`**        | Конфигурация для работы с внешним хранилищем (S3/MinIO) для формирования ссылок на ассеты.                                     |

### 4.2. Frontend (Next.js Components & Logic)

| Компонент                 | Описание                                                                                                                  | Технологии                    |
| :------------------------ | :------------------------------------------------------------------------------------------------------------------------ | :---------------------------- |
| **`MannequinViewer.tsx`** | Главный клиентский компонент. Инициализирует **PixiJS** и создает сцену. Загружает Sprite Sheet.                          | PixiJS, Client Component      |
| **`RotationService.ts`**  | Сервис, который слушает события мыши. Вычисляет скорость и **инерцию** вращения. Мапит положение курсора на индекс кадра. | GSAP Draggable, InertiaPlugin |
| **`SKUSelector.tsx`**     | Обрабатывает выбор цвета и модели. При смене цвета — запрашивает новый Sprite Sheet по URL из манифеста.                  | React State                   |

---

## V. Интерактивный Функционал (Детали Реализации)

### 5.1. Анимация Вращения (GSAP)

- **Привязка:** Вьюер привязывается к событию `mousemove`/`touchmove`. Изменение положения мыши по оси X соотносится с индексом кадра.
- **Паттерн:** Использование **GSAP Draggable** для плавной, инерционной прокрутки (скраббинга) кадров. Движение не должно быть мгновенным, а должно плавно ускоряться и затухать.

### 5.2. Смена Цвета и Модели

- **Метод:** При выборе нового цвета/модели в UI, `MannequinViewer` получает новый `skuId`. Компонент **мгновенно меняет текстуру PixiJS Sprite** на новый Sprite Sheet, URL которого взят из JSON-манифеста.
- **UX:** Смена цвета/модели должна быть быстрой и происходить без перезагрузки всей страницы.

### 5.3. Смена Размера (Zoom)

- **Метод:** Размер костюма (`S`, `M`, `L`) отрабатывается через **визуальный масштаб** (`scale`) компонента PixiJS/Canvas.
- **Детали:** Разница в масштабе должна быть минимальной (например, `M` = 1.0x, `L` = 1.02x), чтобы имитировать небольшую разницу в габаритах, не нарушая фотореализм.

---

## VI. Схема Данных (JSON Manifest)

Данный манифест, отдаваемый NestJS, является единственным источником правды для фронтенда.

```json
/api/v1/models/SLIM_FIT/manifest

{
  "modelId": "SLIM_FIT",
  "rotationFrames": 72,
  "frameWidth": 1024,
  "frameHeight": 1400,
  "skus": [
    {
      "skuId": "SLIM_NAVY_WOOL",
      "fabricName": "Navy Blue Wool",
      "colorHex": "#1C2A3A",
      "spriteSheetUrl": "[https://cdn.example.com/assets/slim/slim_navy_72.webp](https://cdn.example.com/assets/slim/slim_navy_72.webp)"
    },
    {
      "skuId": "SLIM_GREEN_VELVET",
      "fabricName": "Green Velvet",
      "colorHex": "#2D5A42",
      "spriteSheetUrl": "[https://cdn.example.com/assets/slim/slim_green_72.webp](https://cdn.example.com/assets/slim/slim_green_72.webp)"
    }
  ],
  "availableSizes": ["S", "M", "L", "XL"]
}
```
